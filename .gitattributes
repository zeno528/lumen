# 自动生成的源码文件强制 LF
# 覆盖 Windows core.autocrlf=true 的 CRLF 转换，避免 TanStack Router 的
# routeTree.gen.ts 每次 vite 重新生成后"内容未变却标修改"的伪差异
*.gen.ts text eol=lf

# 所有文本文件统一 LF 入库（工作树由 autocrlf 按平台转换）
# 防止不同工具/Agent 提交 CRLF 造成整文件伪差异；二进制文件自动跳过
* text=auto
