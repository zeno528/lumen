# 复制本文件为 .env.dev 并填入你的开发值
# .env.dev 会被 air 自动加载（见 .air.toml 的 env_files），且已被 .gitignore 忽略
# 对应 server/config.go 读取的环境变量

# 应用环境：development / production。缺失或非 development 一律按 production 严格对待。
# 本文件是本地开发模板，固定 development；生产环境在服务器上另行配置（见 README 部署章节）。
APP_ENV=development

# 必须是 8081：web/vite.config.ts 的 /api proxy 写死代理到 localhost:8081
PORT=8081

# ⚠️ JWT_SECRET 同时派生 AES 密钥加密 AI/Serper API 密钥，首次确定后不可更改！
# 改值会导致已存的 AI/Serper 密钥无法解密（需在前端重新输入所有密钥）
# development 下允许用下面的默认值（与历史加密数据对齐）；
# production（或未设 APP_ENV）禁止默认值，服务会拒绝启动——必须设强随机值并固定。
JWT_SECRET=lumen-default-secret-change-me

# 首次启动初始化密码（仅 DB 无密码时用一次，之后改密码走设置中心）。
# development 留空会回退 admin；production 禁止空或 admin，服务会拒绝启动。
APP_PASSWORD=your-dev-password

# 可选：SQLite 一致性快照目录。生产默认 /var/backups/lumen；
# development 未配置时使用 data/backups。不要把该目录放到 Web 静态资源下。
# LUMEN_BACKUP_DIR=/var/backups/lumen
